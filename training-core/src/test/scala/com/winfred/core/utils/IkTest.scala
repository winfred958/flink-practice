package com.winfred.core.utils

object IkTest {
  def main(args: Array[String]): Unit = {
    //    IkAnalyzerUtils.getTerms("中华人民共和国").forEach(println)
    //    println("------------------")
    //    IkAnalyzerUtils.getTerms("用户的Spark应用程序运行在Driver上spark-streaming", true).forEach(println)
    //    println("------------------")
    //    IkAnalyzerUtils.getTerms("通过Py4j实现在Python中调用Java的方法").forEach(println)
  }
}
